# Skilvania 技術架構說明

## 🛠️ 使用技術

### 前端框架
- **Next.js 16** (App Router) - React 全端框架
- **React 19** - UI 函式庫
- **TypeScript** - 型別安全

### 樣式與動畫
- **Tailwind CSS 4** - 工具類 CSS 框架
- **Framer Motion** - 動畫函式庫
- **Lucide React** - 圖標庫

### 後端與資料庫
- **Supabase (PostgreSQL)** - 主要資料庫（使用者、課程、進度）
- **MongoDB** - 活動日誌記錄（NoSQL）
- **NextAuth.js v5** - 認證系統（Email/Password + Google OAuth）

### 狀態管理與工具
- **Zustand** - 輕量級狀態管理
- **React Markdown** - Markdown 渲染
- **KaTeX** - 數學公式渲染
- **Sharp** - 圖片處理

---

## 📐 程式碼架構

### 專案結構
```
src/
├── app/                    # Next.js App Router
│   ├── (auth)/            # 認證路由組
│   ├── (dashboard)/        # 儀表板路由組
│   ├── api/               # API 路由（RESTful）
│   ├── courses/           # 學習者課程頁面
│   ├── creator/           # 創作者功能頁面
│   └── community/         # 社群功能頁面
│
├── components/            # 可重用組件
│   ├── ui/                # UI 組件
│   ├── visualization/     # 技能樹視覺化組件
│   └── content/           # 內容編輯組件
│
├── lib/                   # 工具函數與配置
│   ├── auth/              # 認證配置
│   ├── supabase/          # Supabase 客戶端
│   ├── mongodb/           # MongoDB 客戶端
│   └── utils/             # 工具函數
│
└── types/                 # TypeScript 類型定義
```

### 架構模式

#### 1. **分層架構**
- **表現層**: `app/` 路由頁面（Server/Client Components）
- **業務邏輯層**: `app/api/` API 路由
- **資料存取層**: `lib/supabase/`, `lib/mongodb/`
- **組件層**: `components/` 可重用 UI 組件

#### 2. **路由組織**
- **路由組**: `(auth)`, `(dashboard)` 用於邏輯分組
- **動態路由**: `[courseId]`, `[nodeId]` 用於參數化路由
- **API 路由**: RESTful 設計，遵循 REST 原則

#### 3. **認證流程**
```
使用者請求 → Middleware 檢查 Session → 
  ├─ 已登入 → 允許訪問
  └─ 未登入 → 重定向到 /login
```

#### 4. **資料流**
```
前端組件 → API Route → Supabase/MongoDB → 回傳資料 → 更新 UI
```

### 核心功能模組

1. **認證系統** (`app/(auth)/`, `app/api/auth/`)
   - NextAuth.js 整合
   - Supabase Auth 橋接
   - Session 管理

2. **課程系統** (`app/courses/`, `app/api/courses/`)
   - 技能樹視覺化
   - 節點學習
   - 進度追蹤

3. **創作者系統** (`app/creator/`, `app/api/courses/`)
   - 技能樹編輯器
   - 內容編輯
   - 提交審核

4. **社群系統** (`app/(dashboard)/community/`, `app/api/community/`)
   - 使用者配對
   - 訊息系統
   - 學習群組

5. **活動追蹤** (`lib/mongodb/activity.ts`)
   - MongoDB 日誌記錄
   - 使用者行為分析

### 資料庫設計

#### Supabase (PostgreSQL)
- **USER** - 使用者資料
- **COURSE** - 課程
- **NODE** - 學習節點
- **EDGE** - 節點連接
- **USERPROGRESS** - 學習進度
- **ROLES** - 角色定義

#### MongoDB
- **user_activities** - 活動日誌集合
  - 支援彈性結構
  - 用於分析與追蹤

### 安全性
- Middleware 路由保護
- API 路由權限檢查
- 密碼加密 (bcryptjs)
- Session 管理

### 部署配置
- **Vercel** 部署配置 (`vercel.json`)
- 環境變數管理
- Server/Client Components 分離

---

**最後更新**: 2025-01-XX

